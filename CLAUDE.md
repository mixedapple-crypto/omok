# omok

초등 3학년 딸을 위한 **광고·과금·네트워크 요청이 없는** 오목 앱. 1인 대전(AI 쉬움/보통/어려움) + 2인 대전.
규칙은 **자유룰** — 금수 없음, **5목 이상이면 승리**(장목도 승리). 렌주룰 금수를 적용하지 않는다.

`www/`가 Capacitor의 webDir다 — 여기 있는 것이 그대로 APK에 들어간다.
⚠️ `file://`로 열면 ES 모듈도 Web Worker도 CORS로 죽는다. **반드시 서버로 실행**한다(`npm run serve`).

## 트리거 규칙

| 상황 | 사용 |
|---|---|
| 게임 코드 변경 후 검증, "테스트 돌려줘", "제대로 되는지 확인", "다 됐어?" | `omok-verify` 스킬 |
| 단계 완료 선언 전, APK 빌드 전 최종 판정 | `omok-referee` 에이전트 (판정만 — 수리는 하지 않는다) |
| 딸아이 폰에서 실기기 확인 | `cf-tunnel` 스킬 |
| `.claude/` 자산의 생성·수정·점검 | `forge` 스킬 |

## 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
|---|---|---|---|
| 2026-08-13 | 하네스 초기 구축 (검증 스킬 1 + 블랙박스 검증자 1 + 증거 게이트 훅) | `.claude/` | Fable 적대적 검증이 계획 단계에서만 중대 결함 5건 발견 — 자기 산출물의 결함은 자기가 못 본다 |
